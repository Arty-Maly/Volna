# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Volna' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Volna

  target 'VolnaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VolnaUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'
  pod 'Firebase/Crash'
end
